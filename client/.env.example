# API Configuration
# These variables define how the client connects to the backend API

# Protocol (http:// for development, https:// for production)
VITE_PROTOCOL=http://

# Host and Port (localhost:3000 for development)
VITE_HOST=localhost:3000

# API Path (usually 'api')
VITE_PATH=api

# Production Example:
# VITE_PROTOCOL=https://
# VITE_HOST=yourdomain.com
# VITE_PATH=api
